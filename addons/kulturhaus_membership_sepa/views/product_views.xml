<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Add SEPA fields to product form -->
    <record id="view_product_template_form_sepa" model="ir.ui.view">
        <field name="name">product.template.form.sepa</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">
            <notebook position="inside">
                <page string="SEPA Settings" invisible="not membership">
                    <group>
                        <group string="SEPA Configuration">
                            <field name="membership" invisible="1"/>
                            <field name="is_membership_sepa"/>
                            <field name="membership_period_type" invisible="not is_membership_sepa"/>
                            <field name="sepa_batch_month" invisible="not is_membership_sepa"/>
                            <field name="force_sepa_payment" invisible="not is_membership_sepa"/>
                        </group>
                    </group>
                </page>
            </notebook>
        </field>
    </record>
</odoo>